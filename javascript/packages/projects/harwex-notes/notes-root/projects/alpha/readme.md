# Project alpha

A nested folder, so the tree has something to expand to depth three.

- `design.excalidraw` — a second scene, to prove tabs keep their own canvas.
- Editing this file and saving it writes straight through to disk.
