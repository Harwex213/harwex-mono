// Filled in by `DefinePlugin` in `rspack.config.mjs`.

// `true` when the bundle was built with `--env mocked`: the app runs on the in-memory api.
declare const __API_MOCKED__: boolean;

// Base URL of the tRPC backend.
declare const __API_URL__: string;
