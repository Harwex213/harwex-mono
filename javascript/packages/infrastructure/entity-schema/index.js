const DATA_TYPE = {
    STRING: "STRING",
    NUMBER: "NUMBER",
};

const entity = (definition) => {
    const [keys] = Object.entries(definition);

    for (const key of keys) {
    }
};

export { DATA_TYPE, entity };
